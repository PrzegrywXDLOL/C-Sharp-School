<Window x:Class="TicTacToe_Windowed.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicTacToe_Windowed"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="450">

    <Window.Resources>
        <Style TargetType="{x:Type Button}" x:Key="btnXclick">
            <Setter Property="Background" Value="Yellow"/>
        </Style>
        <Style TargetType="{x:Type Button}" x:Key="btnOclick">
            <Setter Property="Background" Value="Orange"/>
        </Style>
        <Style TargetType="{x:Type Button}" x:Key="clear">
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="{x:Type Button}" x:Key="win">
            <Setter Property="Background" Value="Green"/>
        </Style>
    </Window.Resources>
    
    <Grid x:Name="board">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width ="1*"/>
            <ColumnDefinition Width ="1*"/>
            <ColumnDefinition Width ="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
    </Grid>
</Window>
